# Tag 24 — conntrack & iptables Vertiefung

> **Notiz:** Dieser Tag wird conntrack und iptables-Counter vertiefen.
> Aufgehängt aus dem Tag-20 Final Exam — Fragen 9 und 10 haben gezeigt dass hier Lücken sind:
> - `conntrack -L` vs `iptables -t nat -L` — was zeigt was?
> - iptables `pkts`/`bytes` Counter lesen und interpretieren
> - Verbindungsstatus in conntrack verstehen (`NEW`, `ESTABLISHED`, `RELATED`)

Inhalt wird on-demand erstellt wenn Tag 23 abgeschlossen ist.
